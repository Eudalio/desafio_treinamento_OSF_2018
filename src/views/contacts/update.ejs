<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Update Contato</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css">
    <style>
        .main {
            flex: 1;
            width: 50%;
            align-items: center;
            margin: 7% 0 0 25%;
            align-self: auto;
        }
    </style>
</head>
<body>
    <article>
        <% console.log(contact) %>
        <div class="main jumbotron">
            <h2>Update Contact Form</h2>
            <form action="/contacts/<%= contact._id %>" method="PUT" class="form-signin" id="update-form">
            
                <div class="form-group">
                    <label for="inputName">First name</label>
                    <input type="text" name="name" value="<%= contact.name %>" id="inputName" class="form-control" placeholder="Insert your first name" required>
                </div>

                <div class="form-group">
                    <label for="inputEmail">Email address <small class="form-text text-muted" >(Opcional)</small></label>
                    <input type="email" name="email" value="<%= contact.email %>" id="inputEmail" class="form-control" placeholder="Insert Email address" required autofocus>
                </div>
            
                <div class="form-group">
                    <label for="inputPhone">Phone Number</label>
                    <input type="number" name="phone" value="<%= contact.phone %>" id="inputPhone" class="form-control" placeholder="88999999999" required autofocus>
                </div>

                <div class="form-group">
                    <label for="inputGender">Gender</label>
                    <select name="gender" class="form-control" id="inputGender">
                        <option name="Masculino" value="Masculino">Masculino</option>
                        <option name="Feminino" value="Feminino">Feminino</option>
                    </select>
                </div>
                
                <button class="btn btn-lg btn-primary btn-block" type="submit">Register Contact</button>
            </form>
        </div>
    </article>
    
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script>
         $('#update-form').submit(function(){
            $('#update-form').attr('action', '/contacts/'+$("#id").val());
        });
    </script>
</body>
</html>